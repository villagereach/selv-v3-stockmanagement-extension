version: "2"
services:
  stockmanagement-extension:
    image: openlmis/dev:10
    volumes:
      - '.:/app'
      - 'gradlecache:/gradle'
    env_file: .env
volumes:
  gradlecache:
    external: false